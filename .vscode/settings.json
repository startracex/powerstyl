{
  "files.exclude": {
    "node_modules/": true,
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.map": {
      "when": "$(basename)"
    },
    "**/*.d.ts": {
      "when": "$(basename).ts"
    },
  }
}